# Abstract Factory Design Pattern

The Abstract Factory Pattern provides an interface for creating families of related objects without specifying their concrete classes. It is used to enforce consistency in object creation across different parts of an application.

The Abstract Design Pattern is also interpreted as 
1. Abstract Factory
2. Template Method
3. Bridge 

It is used in scenarios where you need flexibility, decoupling, and code reusability.
